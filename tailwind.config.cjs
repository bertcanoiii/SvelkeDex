const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        'poke-dex-red': '#cc2d3a'
      }
    },
  },

  plugins: [],
};

module.exports = config;
